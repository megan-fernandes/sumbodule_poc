{
  "name": "prisma_npm_poc",
  "version": "1.0.1",
  "description": "making a db schema npm package",
  "main": "index.js",
  "scripts": {
    "generate": "npx prisma generate",
    "postinstall": "npm run generate"
  },
  "devDependencies": {
    "@prisma/client": "^5.5.2",
    "prisma": "^5.5.2"
  },
  "peerDependencies": {
    "@prisma/client": ">=5"
  }
}

